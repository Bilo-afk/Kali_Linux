# 📘 Kali Linux Basics

## 🟢 Section 1: System Basic Commands

This section covers the most important **basic system commands** in Kali
Linux.\
هذا القسم يشرح أهم **أوامر النظام الأساسية** في كالي لينكس.

------------------------------------------------------------------------

# 1️⃣ `pwd`



Print Working Directory --- Shows your current directory path.



يعرض المسار الكامل للمجلد الذي تعمل بداخله حالياً.

``` bash
pwd
```

------------------------------------------------------------------------

# 2️⃣ `ls`



Lists files and directories in the current location.


يعرض الملفات والمجلدات داخل المجلد الحالي.

``` bash
ls
```

Advanced usage:

``` bash
ls -la
```

------------------------------------------------------------------------

# 3️⃣ `cd`



Change Directory --- Move between folders.


يُستخدم للتنقل بين المجلدات.

``` bash
cd Documents
```

Go back:

``` bash
cd ..
```

Go to home:

``` bash
cd ~
```

------------------------------------------------------------------------

# 4️⃣ `clear`



Clears the terminal screen.



ينظف شاشة التيرمنال.

``` bash
clear
```

Shortcut: Ctrl + L

------------------------------------------------------------------------

# 5️⃣ `history`



Shows previously used commands.


يعرض جميع الأوامر التي كتبتها سابقاً.

``` bash
history
```

Run by number:

``` bash
!25
```

------------------------------------------------------------------------

# 6️⃣ `whoami`



Displays the current logged-in user.


يعرض اسم المستخدم الحالي.

``` bash
whoami
```

------------------------------------------------------------------------

# 7️⃣ `id`



Shows UID, GID and groups.



يعرض رقم المستخدم والمجموعات التابعة له.

``` bash
id
```

------------------------------------------------------------------------

# 8️⃣ `uname -a`


Shows detailed system information.


يعرض معلومات كاملة عن النظام.

``` bash
uname -a
```

------------------------------------------------------------------------

# 9️⃣ `hostname`


Displays the system hostname.


يعرض اسم الجهاز.

``` bash
hostname
```

------------------------------------------------------------------------

# 🔟 `date`


Shows current date and time.


يعرض التاريخ والوقت الحالي.

``` bash
date
```

------------------------------------------------------------------------

# 1️⃣1️⃣ `cal`


Displays a calendar.


يعرض تقويم الشهر الحالي.

``` bash
cal
```

------------------------------------------------------------------------

# 1️⃣2️⃣ `uptime`


Shows system running time.


يعرض مدة تشغيل النظام.

``` bash
uptime
```

------------------------------------------------------------------------

# 1️⃣3️⃣ `man`


Opens manual documentation.


يفتح دليل الاستخدام لأي أمر.

``` bash
man ls
```

Exit with:

``` bash
q
```

------------------------------------------------------------------------

# 1️⃣4️⃣ `info`


Alternative detailed documentation system.


نظام شرح تفصيلي لبعض الأوامر.

``` bash
info bash
```

------------------------------------------------------------------------

# 1️⃣5️⃣ `echo`


Print text to terminal.


يطبع نص في التيرمنال.

``` bash
echo Hello
```

Save to file:

``` bash
echo "test" > file.txt
```

------------------------------------------------------------------------

# 1️⃣6️⃣ `alias`


Creates a shortcut command.


ينشئ اختصار لأمر طويل.

``` bash
alias ll='ls -la'
```

------------------------------------------------------------------------

# 1️⃣7️⃣ `unalias`


Removes a shortcut.


يحذف الاختصار.

``` bash
unalias ll
```

------------------------------------------------------------------------

# 1️⃣8️⃣ `exit`


Closes terminal session.


يغلق جلسة التيرمنال.

``` bash
exit
```

------------------------------------------------------------------------

# 🧠 Section Summary

### ✅ You learned:

-   Navigation commands\
-   System information commands\
-   Terminal control\
-   Documentation usage\
-   Command shortcuts

### ✅ تعلمت:

-   أوامر التنقل
-   معلومات النظام
-   التحكم بالتيرمنال
-   قراءة الدليل
-   إنشاء اختصارات
